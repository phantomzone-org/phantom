[workspace]
resolver = "2"
members = [
    "fhevm",
    "compiler",
    "runtime",
    "macros"
    ]

# Workspace-wide dependencies and settings can go here
[workspace.package]

[workspace.dependencies]
# Add any shared dependencies here that multiple crates might use